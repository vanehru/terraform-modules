# Environment Configuration Template
# Copy this file to .env.production and update with your actual values after Terraform deployment

# Backend API URL (will be updated by configure-frontend.sh script)
VUE_APP_API_BASE_URL=https://your-function-app-name.azurewebsites.net/api

# Environment
VUE_APP_ENVIRONMENT=production

# Note: This template file is committed to git
# The actual .env.production file should not be committed (see .gitignore)
